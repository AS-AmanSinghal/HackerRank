#include<stdio.h>
int main()
{
    int n,i;
    signed long long int a[200000],sum=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        scanf("%lld",&a[i]);
    for(i=0;i<n;i++)
        sum+=a[i];
    printf("%lld",sum);
}
